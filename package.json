{
    "name": "crawl-data",
    "dependencies": {
        "@dataform/core": "3.0.6"
    },
    "scripts": {
        "start": "dataform run",
        "compile": "dataform compile",
        "test": "dataform test",
        "format": "dataform format"
    }
}
